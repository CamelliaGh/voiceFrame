// DISABLED: RealTimePreview component to prevent excessive API calls
// This component was causing timeout issues due to frequent preview generation
// Users can now use the manual preview in the PreviewSection step instead

// This file is intentionally left empty to prevent any accidental usage
// The original RealTimePreview functionality has been removed to improve performance
// and prevent timeout issues caused by excessive API calls during customization.

export default function RealTimePreview() {
  return null;
}
